import React from 'react';
import './Header.scss';

const Header: React.FC = () => (
  <header className="header">
    <div className="header__logo">AQVEX</div>
  </header>
);

export default Header;
